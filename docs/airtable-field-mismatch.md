# Airtable Field Mismatch Diagnosis

## Current Status

Your SiteOwlQA pipeline is configured to write to these Airtable fields:

| Field | Purpose | Status |
|-------|---------|--------|
| Processing Status | PASS/FAIL/ERROR | ✓ Configured |
| Score | Numeric percentage (e.g. "95.6%") | ✓ Configured |
| Fail Summary | Detailed failure reasons | ✓ Configured |
| Notes for Internal | Diagnostic details | ✓ Configured |
| True Score | Raw numeric score | ✓ Configured |

## Your Issue

You mentioned: **"Missing columns 'Comments' to 'True Score' for submissions 46 and 47"**

### Possible Cause

Your Airtable base likely has a field named **"Comments"** that isn't defined in our configuration.

## Solution

Choose one of these options:

### Option 1: Add 'Comments' Field to Pipeline (Recommended)

If your Airtable has a "Comments" field that should be filled by the pipeline:

1. **Edit** `src/siteowlqa/config.py`
   
   Find the `AirtableFields` dataclass and add:
   ```python
   comments: str = "Comments"
   ```

2. **Edit** `src/siteowlqa/poll_airtable.py`
   
   In the `airtable.update_result()` call (around line 225), add:
   ```python
   comments="Generated by pipeline",  # or whatever value you want
   ```

3. **Edit** `src/siteowlqa/airtable_client.py`
   
   In the `update_result()` method (around line 270), add `comments` to the fields being written:
   ```python
   if inc_comments and comments:
       f[FIELDS.comments] = comments
   ```

4. **Restart** the pipeline:
   ```bash
   stop_pipeline.bat
   start_pipeline.bat
   ```

### Option 2: Check Why Submissions 46-47 Are Blank

If "Comments" shouldn't be filled by the pipeline:

1. **Check the logs** for submissions 46 and 47:
   ```
   C:\SiteOwlQA_App\logs\siteowlqa.stdout.log
   ```
   
   Search for "submission 46" or "submission 47" or look for ERROR entries

2. **Check Airtable** - these submissions might have:
   - Failed to download
   - Failed validation
   - Had other processing errors

3. **Re-process** them by clearing the "Processing Status" field in Airtable and waiting for the next poll cycle

### Option 3: Verify Your Airtable Schema

To see exactly what columns your Airtable base has:

1. Open your Airtable base in browser
2. Look at the column headers
3. Note any fields between submission start and "True Score"
4. Report back with the exact field names

## Debugging Command

I've created a diagnostic script. Run this:

```bash
python -u scripts/analyze_airtable_config.py
```

This shows exactly what fields the pipeline is configured to write.

## Need Help?

Provide the following information:

1. **Exact column names** in your Airtable (screenshot or list)
2. **Log entries** for submissions 46 and 47 from:
   `C:\SiteOwlQA_App\logs\siteowlqa.stdout.log`
3. **What you expect** in the "Comments" field

Then we can fix it properly! 🐕
