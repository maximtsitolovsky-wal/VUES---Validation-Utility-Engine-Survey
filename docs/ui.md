# UI Assets

Place the executive dashboard HTML and its assets here.

## Assets

Put the camera images referenced by the dashboard into the `assets/` subdirectory.

Expected filenames (edit the HTML if yours differ):
- `camera_top_housing.png`
- `camera_mounting_ring.png`
- `camera_lens_module.png`
- `camera_assembled.png`

For current project roadmap, active development status, and commit workflow, refer to [`development.md`](../development.md).

Place the executive dashboard HTML and its assets here.

## Assets

Put the 4 camera images referenced by the dashboard into:

`ui/assets/`

Expected filenames (edit the HTML if yours differ):
- `camera_top_housing.png`
- `camera_mounting_ring.png`
- `camera_lens_module.png`
- `camera_assembled.png`
