# VUES — Python 3.11 dependencies
# Install with: pip install -r requirements.txt

# Airtable API
pyairtable>=2.3.3

# Data processing
pandas>=2.2.0
openpyxl>=3.1.2      # XLSX read/write
pywin32>=306         # Windows COM automation (Excel)

# BigQuery
google-cloud-bigquery>=3.11.0
google-auth>=2.28.0
db-dtypes>=1.2.0     # required by bigquery.to_dataframe()

# HTTP
requests>=2.31.0
httpx>=0.27.0

# LLM / AI
pydantic-ai>=0.0.24
openai>=1.51.0

# Environment
python-dotenv>=1.0.1

# Standard library modules used (no install needed, listed for clarity):
# smtplib, csv, json, logging, pathlib, dataclasses, typing, shutil
