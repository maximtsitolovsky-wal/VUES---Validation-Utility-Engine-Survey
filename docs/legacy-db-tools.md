# Legacy Database Tools

Proc-era SQL migration, audit, and forensic helpers.

These files are **not part of the active SiteOwlQA grading path**.
Current production behavior is:
- Python owns grading
- SQL Server only supplies site-scoped reference rows

Do not import these scripts into active pipeline code.
Do not reference them as the source of truth for grading logic.
They are retained only for historical DB investigation or one-off maintenance.

For current project roadmap, active development status, and commit workflow, refer to [`development.md`](../development.md).

Proc-era SQL migration, audit, and forensic helpers live here.

These files are **not part of the active SiteOwlQA grading path**.
Current production behavior is:
- Python owns grading
- SQL Server only supplies site-scoped reference rows

Do not import these scripts into active pipeline code.
Do not reference them as the source of truth for grading logic.
They are retained only for historical DB investigation or one-off maintenance.
